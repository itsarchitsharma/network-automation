# GridEdge Energy — Ansible Inventory
# =====================================
# SCALING TO PRODUCTION:
# Add one line per device under [core_routers] or create
# new groups (e.g. [edge_routers]) for different device roles.
# Example:
#   GRIDEDGE-EDGE-01 ansible_host=10.255.1.1
#   GRIDEDGE-EDGE-02 ansible_host=10.255.1.2
#
# Credentials can be set per group in [groupname:vars]
# or per device by adding ansible_user= ansible_password= inline.

[core_routers]
GRIDEDGE-CORE-01 ansible_host=devnetsandboxiosxec8k.cisco.com

[core_routers:vars]
ansible_user=architsharma08
ansible_password=6G-5W7Z_hsna
ansible_network_os=ios
ansible_connection=network_cli
ansible_become=yes
ansible_become_method=enable