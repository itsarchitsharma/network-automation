---
# GridEdge Energy — Host Variables for GRIDEDGE-CORE-01
# ======================================================
# SCALING TO PRODUCTION:
# Create one file per device in this directory.
# Filename must match the hostname in ansible/hosts.ini exactly.
# Example: GRIDEDGE-EDGE-01.yml, GRIDEDGE-EDGE-02.yml
#
# The migration playbook reads these automatically —
# no playbook changes required when adding new devices.

loopback_id: 0
loopback_ip: 10.255.0.1
loopback_mask: 255.255.255.255
bgp_as: 65001
router_id: 10.255.0.1
site: sydney
role: core
legacy_platform: SDH/PDH
new_platform: MPLS-TP